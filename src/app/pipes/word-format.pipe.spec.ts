import { WordFormatPipe } from './word-format.pipe';

describe('WordFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new WordFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
